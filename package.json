{
  "name": "jsql",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Query piped JSON with SQL",
  "packageManager": "bun@1.3.10",
  "engines": {
    "node": ">=24",
    "bun": ">=1.3.10"
  },
  "scripts": {
    "build": "bun build.ts",
    "check": "tsc --noEmit",
    "test": "bun run build && bun test",
    "bench": "bun run build && bun bench/bench.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.10",
    "scriptc": "0.0.36",
    "typescript": "5.9.3"
  }
}
